node: "24"
python: "3.12"
helm: "v4.2.0"
# helmkit is referenced in .github/workflows/validate.yml but GitHub Actions does not
# support expressions in the `uses:` field, so it must be updated there manually.
helmkit: "v1.1.0"
ansible_core: "2.17.0"
ansible_lint: "24.2.0"
ansible_posix: "1.5.4"
