general:
  root_dns: iglesias.cloud
  dns:
    provider: cloudflare

metallb:
  ipPool: 10.0.0.161-10.0.0.170

grafana:
  storage: 10Gi
  port: 30080

prometheus:
  replicas: 2
  retention: 15d
  storage: 40Gi

alertmanager:
  replicas: 2
  storage: 2Gi

velero:
  prefix: "velero"
  retention: "720h"
  schedule: "0 2 * * *"

openbao:
  storageSize: 5Gi
