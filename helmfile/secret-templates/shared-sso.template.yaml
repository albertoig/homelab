# --- shared-sso ---
sso:
  admin:
    # Shared SSO admin username
    # @visible
    username: ""
    # Shared SSO admin display name
    # @visible
    name: ""
    # Shared SSO admin email
    # @visible
    email: ""
    # Shared SSO admin password
    # @autogen: openssl rand -base64 24 | tr -d '=+/'
    password: ""
  grafana:
    # OAuth2 client ID for Grafana SSO
    # @autogen: openssl rand -hex 16
    client_id: ""
    # OAuth2 client secret for Grafana SSO
    # @autogen: openssl rand -hex 32
    client_secret: ""
  argocd:
    # OAuth2 client ID for ArgoCD SSO
    # @autogen: openssl rand -hex 16
    client_id: ""
    # OAuth2 client secret for ArgoCD SSO
    # @autogen: openssl rand -hex 32
    client_secret: ""
  openbao:
    # OAuth2 client ID for OpenBao SSO
    # @autogen: openssl rand -hex 16
    client_id: ""
    # OAuth2 client secret for OpenBao SSO
    # @autogen: openssl rand -hex 32
    client_secret: ""
  argoWorkflows:
    # OAuth2 client ID for Argo Workflows SSO
    # @autogen: openssl rand -hex 16
    client_id: ""
    # OAuth2 client secret for Argo Workflows SSO
    # @autogen: openssl rand -hex 32
    client_secret: ""
