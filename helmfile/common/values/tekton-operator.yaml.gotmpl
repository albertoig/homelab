# Tekton Operator has no configurable Helm values — it is a vendored, pinned
# upstream release (see charts/tekton-operator). All Tekton behaviour is driven
# by the TektonConfig CR in the tekton-config release. This file exists so the
# common_values_only template has something to load.
