# TektonConfig overrides. Sensible defaults live in charts/tekton-config/values.yaml
# (profile: all, Results disabled, Chains in-toto/in-cluster, pruner keep 5).
# Override here if needed, e.g.:
#
# dashboard:
#   readonly: true
# pruner:
#   keep: 10
#   schedule: "0 3 * * *"
